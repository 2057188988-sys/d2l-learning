# PyTorch 学习记录

> 这份文档解决一个问题：**怎么学，才能让学习成果最终变成简历上的东西。**
>
> 核心思想一句话：**以「装饰简历」的目的去学习——每一步都问自己「这个能写进简历吗」。**

---

## 一、核心思想：以「装饰简历」的方式学习

### 先认清一个事实

**学习笔记本身不是简历内容。**

一万个人都在学李沐的课。简历上写「学完《动手学深度学习》」= 零区分度。

面试官想看的不是**你学了什么**，而是**你做出了什么**。

```
「我在学李沐的课」        → 面试官没反应
「我用它做了个东西，链接在这」 → 面试官会点开看
```

### 所以要转换

```
学  →  记  →  改  →  比  →  上传 GitHub  →  写进简历
              ↑         ↑
          容易停下   真正值钱的地方
```

**把「学」变成「做」，再把「做」变成「能点开的链接」。**

### 判断标准

每学完一节，问自己三个问题：

1. 这东西我**改过**吗？（不是只会照抄）
2. 我有**自己的结论**吗？（哪怕只是一句「Adam 比 SGD 快」）
3. 别人**能点开看**吗？（有链接）

三个都是「是」→ 可以写简历。有一个「否」→ 还只是"完成作业"。

---

## 二、六步工作流

用第 3 章「线性回归」举例，走一遍完整流程。

### 第 1 步：学

跟着视频敲代码，跑通，看到结果。

> 现在你手上有一份能跑的 notebook。**但这只是"完成作业"。**

### 第 2 步：记

在 notebook 里用 Markdown 写几句人话，说明每段在干嘛。

> 别人能看懂你干了啥了。**但只有你自己会打开它。**

### 第 3 步：改 ← 最关键的一步

教材用「房屋面积」预测「房价」。
**换成你自己的数据和问题**，比如用「学习时长」预测「考试分数」。

常用三种改法：

| 改法 | 做法 |
|---|---|
| **换数据** | 不用教材的 Fashion-MNIST，换自己的数据 |
| **换模块** | 把某个层 / 优化器 / 损失函数换掉，看效果变化 |
| **加对比** | 做消融实验，用表格记录结果 |

> 到这一步，这份东西才**真正是你的**，不是照抄的。

**从第 2 步到第 3 步是最难跨的一步**，因为要自己动脑想"我能改什么"。大多数人卡在这里。

### 第 4 步：比

试不同的参数 / 结构，把结果做成表格。

```markdown
## 实验：优化器对比

| 优化器 | 学习率 | 10 轮后准确率 | 耗时 |
|---|---|---|---|
| SGD   | 0.1   | 82.3%        | 12s |
| Adam  | 0.01  | 88.7%        | 14s |
| Adam  | 0.001 | 85.1%        | 14s |

结论：Adam 收敛更快，但学习率过小反而下降。
```

> 到这一步，你有了**自己的结论**。这是面试能聊三分钟的东西。

### 第 5 步：上传 GitHub

推到 GitHub，写清楚 README：这是什么、怎么跑、结果如何。

> 到这一步，面试官**能点开看了**。用 `上传学习记录.bat` 一键完成。

### 第 6 步：写进简历

```
❌ 学习《动手学深度学习》第 3 章
✅ 用线性回归预测 XX，对比 3 种学习率，最佳 RMSE = 0.83   [GitHub 链接]
```

---

## 三、每次学完的记录模板

复制这段，填完即可（五分钟）：

```markdown
### YYYY-MM-DD · 第 X.X 节 章节名

**学了什么**（3 行说清）
- 
- 
- 

**关键代码**
（贴核心几行，不是全贴）

**踩的坑**
- 现象：……
- 原因：……
- 解决：……

**我改了什么**
- 

**我想试的**
- 
```

### 为什么「踩的坑」最重要

面试官问细节时（「你遇到过什么问题？」），你有真实素材可讲。
没有这一栏，你只能说「挺顺利的」——等于没做过。

### 为什么「我想试的」重要

这一栏是**下一步实验的种子**。写着写着，你的项目就有了方向。

---

## 四、三档标准：看你停在第几步

| 档位 | 停在哪 | 手上有什么 | 能写简历吗 |
|---|---|---|---|
| **第一档** | 第 2 步 | 自己的笔记 | ❌ 不能，没人看 |
| **第二档** | 第 4 步 | 自己的实验和结论 | ⚠️ 能写，但不亮 |
| **第三档** | 第 5 步 | 一个能点开的链接 | ✅ 简历主力 |

**最低要求：每次学习至少走到第 3 步。**
**理想状态：每个月产出至少一个第三档的东西（一个完整项目 / Demo）。**

### 为什么大多数人卡住

因为**第 1、2 步做完，心理上就画句号了**——跑通了、笔记也写了，「今天学完了」。

但简历需要的是**第 4、5 步**。

---

## 五、自动上传到 GitHub

### 一次性设置（只做一次）

**第 1 步：在 GitHub 网页上新建一个仓库**

- 打开 https://github.com/new
- 仓库名建议：`d2l-learning` 或 `pytorch-learning`
- **不要**勾选「Add a README file」（我们本地已有内容）
- 创建后记下仓库地址，形如：`https://github.com/你的用户名/d2l-learning.git`

**第 2 步：在本地关联远程仓库**

在本目录打开终端，运行（把地址换成你自己的）：

```bash
git remote add origin https://github.com/你的用户名/d2l-learning.git
git branch -M main
git push -u origin main
```

第一次 push 会弹出 GitHub 登录窗口，按提示授权即可。

> 以后就不用再管了。

### 之后每次学完：双击 `上传学习记录.bat`

它会自动做三件事：

1. `git add -A` —— 把所有改动加入暂存区
2. `git commit` —— 用当前时间自动生成提交信息
3. `git push` —— 推到 GitHub

**没有改动时会提示「没有需要提交的改动」，不会产生空提交。**

### 想自己写提交信息

不用 bat，手动来：

```bash
git add -A
git commit -m "学习记录：第 3.6 节 softmax，尝试了 Adam 优化器"
git push
```

**提交信息写得越具体越好**——它是你的学习轨迹，也是给面试官看的"勤奋证据"。

---

## 六、简历写法对照

### ❌ 差的写法（全在描述状态）

```
熟悉深度学习基础知识
学习李沐《动手学深度学习》课程，完成全部章节
了解 CNN / RNN / Transformer 原理
```

### ✅ 好的写法（动词 + 数字 + 链接）

```
· 从零实现 LeNet / ResNet / Transformer，代码见 GitHub（xx star）
· 复现 XX 模型并改进 XX 模块，在 XX 数据集上准确率从 A% → B%
· 独立开发 XX 系统，部署于 HuggingFace Spaces，可在线体验：[链接]
```

**关键差别：动词 + 具体数字 + 可验证的链接。**

---

## 七、目录结构建议

```
c:\sites\pytorch\
├── pytorch/                    ← 官方 d2l 源码（只读，不跟踪到 git）
├── my_learning/                ← ⭐ 你自己的学习代码（重点）
│   ├── README.md
│   ├── ch03_linear/
│   └── ch05_convolution/
├── experiments/                ← ⭐ 改造实验（第二档）
│   └── 2026-09-21_adam_vs_sgd/
├── pytorch记录.md              ← 本文档
├── deep_learning_terms.md
├── 上传学习记录.bat
└── .gitignore
```

**原则**：
- 官方源码保持干净（已在 `.gitignore` 中排除）
- 自己的东西独立管理
- 实验按「日期_主题」命名，方便回溯

---

## 八、学完一节的检查清单

- [ ] notebook 里有 Markdown 说明（别人能看懂）
- [ ] 至少做了**一处改动**（换数据 / 换模块）
- [ ] 有一个**对比表格**或结论
- [ ] **踩的坑**记下来了
- [ ] 双击 `上传学习记录.bat` 推送到 GitHub
- [ ] 想一想：**这条能写进简历吗？怎么写？**

最后一条最重要。

---

## 九、针对你的特别提醒

**1. 论文是最大杠杆**

你在做 CCF-C 小论文。最好的策略不是"先学完再做论文"，而是**学到哪个模块就往论文里搬**：

```
d2l 学的注意力机制 → 论文里的 XX 模块 → 消融实验结果 → 简历项目
```

**一份投入，三份产出**（学习笔记 = 论文素材 = 简历亮点）。

**2. 数学师范背景是资产**

- 数学基础扎实 → 能读懂论文公式（做研究的硬门槛）
- 做过代课老师 → 表达和沟通能力强（汇报、面试加分）

别浪费这两点，简历里要体现。

**3. 赴日这条线**

日企非常看重**文档习惯和过程记录**。你那个规范的学习仓库，在日本面试时是加分项，可以主动提。

拿到 N2 后，可以在 GitHub README 加一个日文版简介——小细节，但会让日企面试官眼前一亮。

**4. 别忘了主线是 Agent**

d2l 是地基，不是终点。学完 Transformer 章节后，要往 LLM / Agent 转：

> 用小型模型做向量检索 + LLM 做总结 → RAG 问答系统

**这才是你简历上的主力项目。**

---

## 十、最后一句

**别为了简历过度记录。** 记录是「顺便」，学到手才是目的。

判断标准：

> **这条记录，一个月后你还能看懂吗？面试时能拿它讲三分钟吗？**
>
> 能 —— 就值得记。不能 —— 那是自我感动。