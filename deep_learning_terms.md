# 深度学习核心名词直白解释

这份文档用最通俗的语言（结合计算机 408 基础知识）为你解释图片中的深度学习概念。

---

## 1. 深度学习基础 (Deep Learning Basics)
*   **线性神经网络 (Linear Neural Networks)**：最简单的模型，就像数学里的 `y = ax + b`。在计算机里可以类比为**最基础的加法器电路**，输入什么，按比例加权直接输出。
*   **多层感知机 (MLP)**：在线性网络基础上加了“隐藏层”。类比**多级组合逻辑电路**，通过堆叠简单的逻辑门（与或非），实现复杂的逻辑功能。

## 2. 卷积神经网络 (CNN) - 图像识别的专家
*   **核心逻辑**：就像用**滤镜（卷积核）**去扫描图片的每一个角落，提取特征（边缘、形状）。
*   **演进型号**：
    *   **LeNet**：鼻祖，用于识写数字（像 8086 CPU，开山之作）。
    *   **AlexNet**：大力出奇迹，模型变深了（像 Pentium，性能质跃）。
    *   **VGG**：整齐划一，全是 3x3 小滤镜（模块化设计）。
    *   **Inception**：多路并行，既看大轮廓也看小细节（类似 CPU 的**超标量流水线**，同时执行多个操作）。
    *   **ResNet (残差网络)**：加了“快捷连接”。类比**数据旁路 (Data Bypassing)**，解决信号在深层传递时消失的问题，让网络可以修到上千层。

## 3. 循环神经网络 (RNN) - 处理序列的“记忆者”
*   **RNN**：专门处理有顺序的数据（如文字、气温）。类比**带反馈的寄存器**或**有限状态机 (FSM)**，当前的输出不仅取决于当前输入，还取决于上一步的“状态”。
*   **GRU / LSTM**：给 RNN 加了“门控”机制。类比**缓存置换算法 (LRU)**，决定哪些信息该记住，哪些该忘掉，解决了长序列容易忘事的问题。
*   **seq2seq**：把一个序列转成另一个序列（如中译英）。类比**编译器/汇编器**，把高级语言代码（源序列）翻译成机器码（目标序列）。

## 4. 注意力机制与 Transformer - 现代 AI 的地基
*   **Attention (注意力)**：模型学会“抓重点”。类比**指针或索引查询**，在海量数据中精准定位到最相关的部分。
*   **Transformer**：完全抛弃了 RNN 的顺序结构，改为全面并行。类比**从单核顺序执行进化到多核大规模并行计算**，它是目前 ChatGPT 等大模型的核心引擎。

## 5. 优化算法 - 寻找最优解的策略
*   **核心逻辑**：就像在山顶盲降，目标是走到山谷（误差最小点）。
*   **SGD**：随机选路下山。虽然快，但容易走弯路（像**随机寻道**）。
*   **Momentum (动量)**：下山带点惯性，遇到小坑直接冲过去。
*   **Adam**：智能调节脚步大小。类比**自适应传输协议**，路平就大步跑，路颠就小步挪。

## 6. 高性能计算 - 让训练跑得飞快
*   **并行 (Parallelism)**：多个计算单元同时开工。类比**多线程/多进程**。
*   **多 GPU**：多块显卡一起算。类比**多处理器系统 (SMP)**。
*   **分布式 (Distributed)**：多台机器联网算。类比**计算机集群或云计算**。

## 7. 计算机视觉 (CV) - 给机器装上眼睛
*   **目标检测 (Object Detection)**：不仅要看出是猫，还要在图里框出来。类比**图像检索 + 坐标定位**。
*   **语义分割 (Semantic Segmentation)**：把图片里每个像素都分类（这是路，那是树）。类比**逐像素的内存标记**。

## 8. 自然语言处理 (NLP) - 让机器理解文字
*   **词嵌入 (Word Embedding)**：把单词变成一串数字（向量）。类比**编码 (Encoding)**，把字符变成计算机能理解的二进制数值，且意思相近的词数值也接近。
*   **BERT**：预训练的大力神。它通过“完形填空”学会了语言的上下文。类比**库函数/操作系统的内核**，别人可以直接调用它处理各种复杂的文字任务。
